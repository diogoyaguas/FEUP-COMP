class Fac {

    int c;
    boolean troix;

    public int Quatre(){

        return 2;
    }

    public int ComputeFac(int num, int deux){
        int num_aux ;
        
        num_aux = 1 * 2 + 3;
        
        if (num < 1)
            num_aux = 1 * 2 + 3;
        else
            num_aux = num + (this.ComputeFac(num-1, num_aux));
        
        this.Quatre();
        return num_aux;
    }
    
    public static void main(String[] a){
        io.println(new Fac().ComputeFac(10)); //assuming the existence of the classfile io.class
    }
}